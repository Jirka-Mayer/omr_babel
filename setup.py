import setuptools

setuptools.setup(
    packages=[
        "omr_babel",
    ],
    package_data={},
    include_package_data=False,
    install_requires=[
        # dependencies go here
        # e.g.: "numpy>=1.11.1",
    ]
)